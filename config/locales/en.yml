en:
  hello: "Hello world"

  activerecord:
    errors:
      models:
        user:
          attributes:
            name:
              blank: "Name can't be blank"
              too_short: "Name is too short (minimum is %{count} characters)"
              too_long: "Name is too long (maximum is %{count} characters)"
              invalid: "Name is invalid"
            gender:
              inlusion: "Gender is invalid"
            height:
              greater_than: "Height is invalid"
              less_than_or_equal_to: "Height is invalid"
            weight:
              greater_than: "Weight is invalid"
              less_than_or_equal_to: "Weight is invalid"
            experience_level: